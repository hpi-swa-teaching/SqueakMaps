Directed weighted edge in the routing graph.

Connects two vertex indices (from–to) and carries the set of floor levels on which the edge is traversable. Edge weights are derived from the geographic distance between the two connected vertices.