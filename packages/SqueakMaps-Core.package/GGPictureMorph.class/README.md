A GGPictureMorph shows a single image scaled to fit the morph. In
GeoGuessr it displays the current round's location photo and the start-screen
background, and forwards mouse clicks to its gameHandler (the window) so the
picture doubles as a clickable / view-swap surface.